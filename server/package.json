{
  "name": "server",
  "version": "1.0.0",
  "description": "Backend of SimpleDiary",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.2",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "tslint": "^6.0.0",
    "typescript": "^3.7.5"
  }
}
